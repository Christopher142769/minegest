# MongoDB Atlas (obligatoire)
MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/truckers

# Secret JWT pour l'authentification (obligatoire en production)
JWT_SECRET=changez-moi-en-production

# Port (Hostinger définit PORT automatiquement)
PORT=3000
