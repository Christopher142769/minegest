services:
  - type: static_site
    name: minegestion-dash
    rootDir: frontend
    env:
      NODE_VERSION: 18
    buildCommand: "npm cache clean --force && npm install --legacy-peer-deps --verbose && npm run build"
    publishPath: "build"
